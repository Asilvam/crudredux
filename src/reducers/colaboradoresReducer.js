const initialState = {
  colaboradores: [],
  error: null,
  loading: false,
};


const colaboradoresReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default colaboradoresReducer;
